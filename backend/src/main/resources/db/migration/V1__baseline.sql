-- Baseline: asume que las tablas ya existen.
-- Si es una DB nueva, puedes crear estructura aquí.
-- Si ya tienes tablas creadas por Hibernate, Flyway creará el baseline.

-- tables 'users', 'flights', 'reservations' deben existir ya;
-- si no existen, crea CREATE TABLEs aquí.