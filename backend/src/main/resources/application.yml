server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aerotickets?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: nomeselacontra
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: false

# === LOGGING CORPORATIVO ===
logging:
  file:
    name: logs/aerotickets.log
  level:
    root: INFO
    com.aerotickets: DEBUG
    org.springframework.security: DEBUG   # 👈 ver autenticación
    org.springframework.web: DEBUG        # 👈 ver peticiones HTTP
    org.hibernate.SQL: DEBUG              # 👈 ver consultas SQL
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} | %clr(%-5level){cyan} | %clr(%logger{36}){blue} | %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} | %-5level | %logger{36} | %msg%n"

vueler:
  jwt:
    secret: "746B436D49794C75526A536B56566E586B48756A4E5442304E43755963615674"
    expiration-minutes: 120

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized